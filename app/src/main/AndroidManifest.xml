<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.pucminas.rnunez.primeiroapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".exemplo0.PrimeiraActivity"
            android:label="PrimeiraActivity" />
        <activity
            android:name=".exemplo0.SegundaActivity"
            android:label="SegundaActicity" />
        <activity
            android:name=".exemplo1.CalculadoraActivity"
            android:label="Calculadora" />
        <activity
            android:name=".exemplo2.LayoutInflaterActivity"
            android:label="Layout Inflator Ex." />
        <activity
            android:name=".exemplo3.RecursosActivity"
            android:label="Recursos Activity" />
        <activity
            android:name=".exemplo4.ComponentesActivity"
            android:label="Componentes Activity"/>
    </application>

</manifest>